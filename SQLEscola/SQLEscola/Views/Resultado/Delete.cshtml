@model SQLEscola.Banco.tb_resultado

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>tb_resultado</legend>

    <div class="display-label">Erros</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Erros)
    </div>

    <div class="display-label">Comentarios_Prof</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Comentarios_Prof)
    </div>

    <div class="display-label">tb_resposta</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.tb_resposta.ScriptResposta)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
