@model SQLEscola.Banco.tb_ativar_perfil

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>tb_ativar_perfil</legend>

    <div class="display-label">tb_usuario</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.tb_usuario.Nome)
    </div>

    <div class="display-label">Status</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Status)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.Id_Ativar_Perfil }) |
    @Html.ActionLink("Back to List", "Index")
</p>
