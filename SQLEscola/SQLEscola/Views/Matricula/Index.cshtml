@model IEnumerable<SQLEscola.Banco.tb_matricula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            tb_usuario
        </th>
        <th>
            tb_turma
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tb_usuario.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_turma.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Matricula }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Matricula }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Matricula })
        </td>
    </tr>
}

</table>
